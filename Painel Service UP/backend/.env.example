# Copie para .env e preencha (nunca commitar .env).
# Em produção: configurar variáveis no Azure Repos (Pipelines / Variable groups ou Key Vault).

# MYSQL (dw_combio)
DB_HOST=localhost
DB_PORT=3306
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
DB_NAME=dw_combio

# API
PORT=3000
NODE_ENV=development